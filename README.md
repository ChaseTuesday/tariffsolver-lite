# TariffSolver-Lite ⚙️
**Instant AI Tariff Classification Demo**

TariffSolver-Lite is the public demo of TariffSolver’s classification engine — showing how merchants can automatically determine HS/HTS codes, duty rates, and tariff logic in seconds.

## 🚀 Run locally
1. Install dependencies:
   ```bash
   pip install -r requirements.txt
   ```
2. Run the Streamlit app:
   ```bash
   streamlit run app/main.py
   ```

## 🔑 Add your OpenAI API Key
In the sidebar of the app, paste your API key to enable AI-powered classification.
